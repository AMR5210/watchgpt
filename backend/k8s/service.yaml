apiVersion: v1
kind: Service
metadata:
  name: watchgpt-backend
  namespace: watchgpt
spec:
  type: ClusterIP
  selector:
    app: watchgpt-backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
